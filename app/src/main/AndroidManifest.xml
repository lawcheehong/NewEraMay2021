<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.neweramay2021">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewEraMay2021">
        <activity android:name=".sharedpreferences.SharedPreferencesDisplayActivity"></activity>
        <activity android:name=".sharedpreferences.SharedPreferencesDemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".studentrecycleview.StudentlistActivity"></activity>
        <activity android:name=".recycleview.RecycleViewActivity" />
        <activity android:name=".listview.Contact" />
        <activity android:name=".listview.SimpleListviewActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>